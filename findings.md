# 盘点与发现

## 已确认产品边界

- 产品是“个人数字杂志 + Hi-Fi 研究与试听档案”，作者身份是“Hi-Fi 行业观察者与内容创作者”。
- 一级导航固定为首页、行业研究、试听档案、摄影、关于。
- 首页顺序固定为个人封面、本期关注、试听档案、行业研究、摄影精选、认识我。
- 第一版不包含登录、评论、点赞、评分、排行、商城、复杂 CMS 或数据库。
- 技术基线固定为 Astro + TypeScript + Tailwind + Content Collections/Zod + 受控 Obsidian 同步 + 必要 React Islands + ECharts + Sharp + Cloudflare Workers + pnpm。

## 内容真实性

- 所有个人、试听、现场、摄影、研究、平台、联系和简历内容必须来自用户真实素材。
- 未准备好的内容可在本地预览标记待补充，正式构建必须排除。
- Obsidian 仅消费 `网站发布/` 中 `publish: true` 的内容，并阻止私人引用、未发布附件和整个 Vault 扫描公开。

## 阶段 0 待核实

- 当前工作区是否已有项目文件与用户未提交修改。
- Obsidian Vault 是否已有 `网站发布/`。
- 七份真实研究的所在位置与内容完整性。
- 作者本人照片、参展照片、试听照片、摄影作品、个人简介、平台链接、公开联系方式和 PDF 简历是否已提供。
- `gpt-taste` Skill 是否可完整读取（视觉阶段前将再完整加载并执行预检）。

## 当前目标目录与环境

- 工作目录在盘点开始时没有项目文件，仅有本次创建的 `task_plan.md`、`findings.md` 和 `progress.md`。
- 目录尚不是 Git 仓库，因此不存在需要保留的已跟踪或未提交代码改动。
- 已检测 Node.js `v24.15.0` 与 pnpm `11.6.0`。
- 当前 Windows 默认命令入口存在 CET 兼容问题，但可通过另一可用入口完成后续工作。
- Git `2.54.0.windows.1` 可用，但当前目录尚未初始化仓库。
- 项目指定的 `gpt-taste/SKILL.md` 存在；将在阶段 5 进入 UI 实现前完整加载并执行它的 `<design_plan>` 预检。
- Obsidian CLI 当前无法连接，因为 Obsidian 桌面端未运行；阶段 0 盘点改用文件系统只读检查，不会触发 Vault 写入。

## 阶段 0 内容盘点结果

- 七份研究均存在于 Obsidian Vault 根目录，文件名与规格标题匹配。
- Vault 中不存在 `网站发布/`，全部 Markdown 也都没有 `publish:` 字段；因此当前没有任何内容被授权公开。
- 三份研究缺少 `research_date`、`imported`、`source_type`、`source_file`；其余四份的 `source_file` 指向不存在的 DOCX。
- 实际存在的非 Markdown 附件只有一份 PC 渠道策略 DOCX 和一份山灵产品资料 XLSX。
- 多份研究互相链接，并链接 `首页.md` 与 `山灵产品表格.md`；同步必须用显式白名单，不能顺着 Wikilink 递归公开辅助笔记或 XLSX。
- 部分研究包含公开事实、作者判断和待验证假设的混合表达，页面映射时需保留免责声明并显式区分。
- 行业分析目录可确认一份 PC 渠道策略正式版和一份山灵全产品表；其他相关报告是派生材料，不能冒充七份研究原件。
- Vault 中未发现作者本人照片、参展／探店／试听照片、摄影精选、个人简介、平台账户、可公开联系方式或 PDF 简历。
- `.trash/` 与 `.obsidian/` 必须在所有同步与构建流程中硬性排除。

## 阶段 3 内容接入结果

- `preview: true` 只授权本地预览，不等同于正式发布；生产同步与静态构建只显示 `publish: true`。
- 七份研究副本已有完整网页元数据和稳定 URL，正文与公开来源得到保留，原始笔记没有被修改。
- 研究副本之间的公开 Wikilink 可转换为站内链接；私人首页、内部产品表和未公开 Word 附件不会形成可点击公开引用。
- 两份策略稿仍明确包含“待补充”的来源或验证缺口，因此即使未来改成 `publish: true`，正式模式也会在补齐前阻止构建。
- 销售经理研究中的利润率、渠道权重、模拟话术和情景数据属于估算／推断／场景模拟，页面必须保留正文中的证据属性说明，不能展示成已核验经营数据。
- 销售经理研究副本中发现一条与全文无关的英文医疗免责声明，已作为导入噪音从发布副本清除；原始笔记保留不变。

## 页面与质量验证结果

- 内容日期必须按 UTC 格式化；若直接使用运行机器的芝加哥时区，`2026-08-27` 会错误显示成前一天。已使用统一日期格式器修复全部页面。
- React Island 由服务端先渲染，GSAP 不应在其水合前修改子节点属性；研究结果卡改用独立 CSS 交互，避免水合不一致。
- 本地开发版可查看七篇预览研究；生产同步预演选择 0 篇，并将七个预览生成文件列为生产清理对象，证明预览与发布隔离有效。
- 生产静态首页 Lighthouse 得分为 100 / 100 / 96 / 100（性能 / 可访问性 / 最佳实践 / SEO）。
- 当前空内容警告对应真实素材未提供，不是构建错误；页面以明确空状态呈现，不生成占位人物、试听或摄影内容。

## 首页三版静态风格探索

- 三版共用同一份语义内容模板，栏目、文字与内容顺序一致；差异仅来自主题 CSS 和视觉构图。
- A 版呈现米白编辑研究刊物，研究栏目以荧光绿整版强调；B 版呈现深色器材面板与克制的功能箭头；C 版以暖象牙、暗红和抽象接触印突出“一些随手拍”。
- 三张成图均为 1440px 宽，H1 均为两行，无横向溢出、页面脚本错误或虚构摄影内容。
- Phosphor 官网仓库是独立 vendor 项目，不能参与本站根 TypeScript 检查；根配置只排除 `vendor/`，不改变正式网站构建行为。

## 照片基调延展探索

- 用户提供的黑白城市照片具有低曝光暴雨云层、远景天际线与近景杯子三层景深，适合形成“个人观察现场”而不是器材品牌感。
- D 版采用照片全幅首屏、局部研究横幅和摄影模块裁切；其余界面使用煤黑、雾白、冷银与一段浅灰研究章节，避免全页暗色导致层级消失。
- D 版内容与 A／B／C 一致，长图为 1440×4518px，H1 单行，无横向溢出或页面脚本错误；照片已复制到隔离原型资源目录，不依赖微信临时文件路径。
## 试听档案真实内容接入

- 用户提供两条真实试听原文及对应照片：天使吉米氧气（入耳）与 Hifiman EF500／EDXS（大耳）。
- 内容保留作者主观判断，不添加评分或虚构参数；照片归档在 `src/content/listening/assets/`，详情页使用 Astro 图片处理。
- 两条记录当前为本地预览状态，正式构建不会未经确认公开。
## 试听详情单图环绕排版

- 详情页现在只渲染 `cover` 一张图片；`photoNotes` 不再重复输出，试听记录中也移除了 `finalImpression` 字段。
- 通过 Astro `render(entry)` 将用户原文作为正文插槽接入，桌面端使用浮动图文环绕，移动端取消浮动以保证阅读顺序。
- 详情页截图已实际复核，标题摘要间距、图片加载和正文宽度均正常。
- 用户截图对应的是 `/listening/in-ear/` 分类列表页；具体档案需点击其中的标题进入 `/listening/in-ear/oxygen-oxygen/`。分类层级按既定信息架构保留。

## 2026-09-03 Obsidian 更新与研究移除

- Obsidian CLI 可用且桌面端正在运行；当前 Vault 为 `D:\我的文档\Documents\Obsidian Vault`。
- `网站发布/行业研究/` 当前有 7 篇研究，包含 `中国Hi-Fi行业销售经理研究.md`。
- 用户要求删除网站中的该研究；当前判断为网站侧移除，保留 Obsidian 原文，避免未经授权修改 Vault。
- 网站本地内容目前仍有 `src/content/research/china-hifi-sales-manager.md`，其余研究正文和 QA 脚本存在多处指向该 URL，移除时需要同步清理关联引用与验证入口。
- 现有同步逻辑会选择所有 `publish: true` 或开发环境 `preview: true` 的内容，因此需要增加网站侧排除清单；否则下一次同步会重新生成该研究。
- 目标研究在 Obsidian 发布区仍为 `publish: false`、`preview: true`，本次不会修改该 Vault 文件。
- 网站排除清单已加入同步配置；被排除研究的关联元数据会被过滤，正文中的 Wikilink 和 `/research/.../` Markdown 链接会降级为普通文本。
- 普通同步已实际更新 6 篇研究并清除目标本地文件；生产同步预演为 0 篇正式内容，符合当前所有研究仍未授权发布的门禁。
- 网站研究列表当前应显示 6 篇；UI 测试原先固定断言 7 篇，已更新为 6 篇。
- 最终验证：目标研究 URL 和标题不再出现在 `src`／`dist`；Obsidian 发布区原文仍可通过 CLI 定位，证明本次只做网站侧删除。
- 最终质量结果：同步单元测试 6/6、Astro 检查 0/0/0、生产构建 8 页、UI 回归 3 通过／1 跳过。
