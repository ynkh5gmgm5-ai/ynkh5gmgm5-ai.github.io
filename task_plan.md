# Hi-Fi 个人品牌网站开发计划

## 目标

依据已确认的设计规格，完成一个可维护、可预览、可发布的 Hi-Fi 个人品牌网站；公开内容只来自明确授权的真实素材，Obsidian 私人内容不得进入公开构建。

## 唯一产品依据

- `D:\我的文档\Documents\行业分析\docs\superpowers\specs\2026-08-27-hifi-personal-brand-website-design.md`
- 执行约束：`D:\我的文档\Documents\行业分析\docs\website-agent\AGENTS.md`

## 阶段

### 阶段 0：只读盘点

**状态：已完成**

- 阅读执行约束与完整设计规格。
- 盘点目标目录、运行环境、Git 状态和可用视觉 Skill。
- 只读盘点 Obsidian 中的七份研究、图片、简历、平台资料与发布区状态。
- 输出内容缺口、风险与执行顺序，不修改 Vault。

### 阶段 1：项目与内容骨架

**状态：已完成**

- 建立 Astro + TypeScript + Tailwind 项目。
- 建立固定路由、Content Collections、Zod 模型与明确错误。
- 保持页面结构正确，不提前进入最终视觉。

### 阶段 2：Obsidian 受控同步

**状态：已完成**

- 实现受控读取、转换、附件复制和校验。
- 验证 Wikilink、图片嵌入、Callout 和私人引用阻断。

### 阶段 3：真实内容接入

**状态：已完成（当前已有素材范围）**

- 七份研究已整理为本地预览稿并完成受控同步，仍全部保持 `publish: false`。
- 试听、现场、摄影、个人资料和简历尚未提供，因此对应栏目保持空状态，不生成虚构内容。

### 阶段 4：页面与交互

**状态：已完成**

- 完成固定信息架构、搜索、筛选、关联内容、图片查看和移动端阅读顺序。

### 阶段 5：视觉实现

**状态：已完成（真实素材到位前的可交付版本）**

- 进入 UI 实现前完整读取 `gpt-taste` Skill，先产出强制 `<design_plan>` 预检。
- 使用真实作者素材建立编辑式视觉与有节制的 GSAP 动效。

### 阶段 6：质量验证

**状态：已完成（当前已有内容范围）**

- 完成类型、内容、构建、Playwright、链接、图片、桌面／手机实际页面、Lighthouse、EXIF 与私人信息检查。

### 阶段 7：预览与发布

**状态：等待用户验收与真实素材**

- 提供可访问预览与验收清单。
- 只在用户明确确认后进行正式发布。

### 阶段 8：首页三版静态风格探索

**状态：已完成**

- 使用当前真实首页内容制作 A／B／C 三版隔离 HTML/CSS 原型。
- 生成统一 1440px 宽完整首页长图到 `qa/style-explorations/`。
- 校验内容一致、标题行数、横向溢出与可读性，不修改正式页面路由、组件或内容。

### 阶段 9：照片基调延展探索

**状态：已完成**

- 使用用户提供的真实黑白城市照片制作 D 版隔离原型。
- 保持与 A／B／C 相同的首页内容和顺序，输出统一 1440px 长图。
- 校验背景文字对比、照片裁切、横向溢出与正式站隔离。

## 不可越过的门禁

- 不虚构任何个人、试听、现场、摄影、研究、账号、联系或履历信息。
- 不修改或公开未经确认的 Vault 内容。
- 不在视觉阶段前擅自引入最终 UI 与重动效。
- 不未经确认部署正式版本。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| Windows CET 兼容性导致默认命令入口失败 | 1 | 改用 `cmd.exe` 且通过目标工作目录读取 Unicode 路径下的文件。 |
| `cmd.exe` 直接接收中文绝对路径产生乱码 | 2 | 不再传入中文路径命令，改用工作目录参数配合相对文件名。 |
| pnpm 安全策略阻止 `esbuild` 安装脚本 | 1 | 依赖已下载；在 `pnpm-workspace.yaml` 中仅将 `esbuild` 加入允许构建白名单后重试。 |
| Astro 尝试写入用户级遥测配置目录而被权限阻止 | 1 | 在所有 Astro 项目脚本中通过 `cross-env` 显式禁用遥测，保持操作只在项目目录内。 |
| `astro check` 首次运行只显示缺少官方检查器的交互提示 | 1 | 增加锁定版本的 `@astrojs/check`，安装后重新执行真实类型检查。 |
| TypeScript 7 尚未向 Astro 检查器提供所需程序化 API | 1 | 按 `@astrojs/check` 官方同伴范围锁定 TypeScript `6.0.3`。 |
| Git 仓库由沙箱账户创建，本机账户检测为不可信所有权并阻断命令环境刷新 | 1 | 未修改全局 Git 安全配置；已删除本轮刚创建且无提交的 `.git` 元数据，保留全部项目文件。 |
| Windows PowerShell 清理 `.git` 时再次命中 CET 兼容错误 | 1 | 先使用只读命令核对 `.git` 绝对路径，再使用同一 Windows 命令环境删除精确目标。 |
| Astro 7 默认 Markdown 引擎不再直接运行 Remark/Rehype 插件 | 1 | 按配置校验的官方提示增加 `@astrojs/markdown-remark`，保留既定 Remark/Rehype 处理边界。 |
| 首次真实 Vault 预演的带空格路径被命令环境拆分 | 1 | 读取 Windows 短路径 `OBSIDI~1` 后重跑只读预演，正确确认 `网站发布/` 不存在且工具未创建它。 |
| YAML 将单日研究日期解析为 Date，而 Schema 最初只接受字符串 | 1 | 研究日期字段同时接受单日 Date 与时间范围字符串，并统一把单日转换为 `YYYY-MM-DD`。 |
| 销售经理研究副本末尾含无关英文医疗免责声明 | 1 | 将其认定为导入噪音，仅从网站发布副本清除，并把规则固化；原始笔记未修改。 |
| 安装 `@gsap/react` 时 pnpm 默认 store 与现有依赖 store 不一致 | 1 | 显式复用项目内 `.pnpm-store`，依赖锁文件再次通过供应链检查。 |
| Astro 配置热重启后开发服务器出现一次 React 模块缓存异常 | 1 | 干净停止并重启开发服务器，后续页面错误监听与交互测试均通过。 |
| GSAP 在 React Island 水合前修改卡片行内样式 | 1 | 将研究筛选结果从全局卡片动效目标中移除，保留 CSS 交互；桌面与手机水合测试通过。 |
| Playwright 的 iPhone 设备配置默认选择未安装的 WebKit | 1 | 明确让移动端项目复用已安装的 Chromium，并保持 iPhone 视口与触控参数。 |
| UI 回归仍断言 7 篇研究 | 1 | 删除一篇研究后更新 `tests/site.spec.ts` 为 6 篇，重新执行回归。 |
| 旧 Astro 开发服务器未完成 React 水合 | 1 | 停止旧的项目开发服务器并干净重启后复跑 UI 回归。 |
### 阶段 10：试听档案真实内容接入

**状态：已完成（等待发布确认）**

- 接入用户提供的两条试听原文、必要元信息及对应照片。
- 分别归入入耳／大耳，保留原文主观判断，不添加评分或虚构参数。
- 本地预览、类型检查、构建和 UI 回归均已验证；正式发布仍需用户确认。

### 阶段 11：Obsidian 内容更新与研究移除

**状态：已完成**

- 从当前 Obsidian 发布区同步用户最新内容变更。
- 保留 Obsidian 原始笔记，仅从网站内容、关联链接和验证脚本中移除“中国Hi-Fi行业销售经理研究”。
- 重新执行同步、类型检查、构建和相关链接／页面验证。
